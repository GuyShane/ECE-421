#!usr/bin/ruby

require 'inotify'

module FileWatch

  def creation(duration, *args)
    notifier = Inotify.new
    args.each do |file|
      notifier.add_watch(file, Inotify::MOVE, Inotify::CREATE)
      yield
    end
  end

end  
